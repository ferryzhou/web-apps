# Value vs Arbitrage — the deep dive

Why the scalable version of "profit from unequal prices" is model-vs-market
pricing (value betting) rather than venue-vs-venue arbitrage: the counterparty
reframe, the mechanics of the arb ceiling and of value scalability, an
interactive crossover simulator (deterministic stake-capped arb path vs
Monte-Carlo Kelly-sized value paths), the maturation ladder, and the closing
line value self-test.

Open `index.html` — no build step, no network calls.
